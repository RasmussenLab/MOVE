user_config: data.yaml  # DEPRECATE
na_value: NA  # DEPRECATE

# raw - for raw data (e.g., TSVs, text)
# interim - for intermediate data (e.g., NPYs, trained models, latents)
# processed - for resultsa (e.g., TSVs)
raw_data_path: data/
interim_data_path: interim_data/
processed_data_path: processed_data/  # RENAME 'results_path'
headers_path: headers/  # DEPRECATE
version: v1  # DEPRECATE

sample_names: baseline_ids
ids_file_name: baseline_ids.txt  # DEPRECATE (RENAMED 'sample_names')
ids_has_header: True  # DEPRECATE
ids_colname: 0  # DEPRECATE

categorical_inputs:
  - name: diabetes_genotypes
    weight: 1
  - name: baseline_drugs
    weight: 1
  - name: baseline_categorical
    weight: 1

continuous_inputs:
  - name: baseline_continuous
    weight: 2
  - name: baseline_transcriptomics
    weight: 1
  - name: baseline_diet_wearables
    weight: 1
  - name: baseline_proteomic_antibodies
    weight: 1
  - name: baseline_target_metabolomics
    weight: 1
  - name: baseline_untarget_metabolomics
    weight: 1
  - name: baseline_metagenomics
    weight: 1

data_of_interest: baseline_drugs  # DEPRECATE

categorical_names: ${names:${data.categorical_inputs}}
continuous_names: ${names:${data.continuous_inputs}}
categorical_weights: ${weights:${data.categorical_inputs}}
continuous_weights: ${weights:${data.continuous_inputs}}

data_features_to_visualize_notebook4:  # DEPRECATE
  - drug_1
  - clinical_continuous_2
  - clinical_continuous_3

write_omics_results_notebook5:  # DEPRECATE
  - baseline_target_metabolomics
  - baseline_untarget_metabolomics 