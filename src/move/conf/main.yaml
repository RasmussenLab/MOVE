# @package _global_

defaults:
  - _self_
  - data: base_data
  - model: vae.yaml
  - tuning_reconstruction: main.yaml
  - tuning_stability: main.yaml
  - training_latent: main.yaml
  - training_association: main.yaml
  - task: null
  - experiment: null
  - override /hydra/job_logging: none #disabling hydra logging and using custom logging instead
  - override /hydra/hydra_logging: none
hydra:
  output_subdir: null
  run:
      dir: ./

name: MOVE
seed: 123456
