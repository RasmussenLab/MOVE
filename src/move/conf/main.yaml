# @package _global_

defaults:
  - _self_
  - data: main.yaml
  - model: vae.yaml
  - training: main.yaml
  - tuning_reconstruction: main.yaml
  - tuning_stability: main.yaml
  - experiment: null

hydra:
  output_subdir: null
  run:
      dir: ./

name: MOVE
seed: 123456
