defaults:
  - encode_data_schema

raw_data_path: ${data.raw_data_path}
interim_data_path: ${data.interim_data_path}

sample_names_filename: ${data.sample_names}

discrete_inputs: ${data.categorical_inputs}
continuous_inputs: ${data.continuous_inputs}
