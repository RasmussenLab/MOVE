defaults:
  - identify_associations_ttest

batch_size: 10

num_refits: 40 # default is 40

target_dataset: random.small.proteomics
target_value: plus_std
save_refits: True

model:
  num_hidden:
    - 1000
  num_latent: 150

training_loop:
  lr: 1e-4
  num_epochs: 40