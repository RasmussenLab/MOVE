na_value: na

# raw - for raw data (e.g., the TSVs)
# interim - for intermediate data (e.g., the NPYs)
# processed - for processed data (e.g., trained models, latents)
raw_data_path: data/
interim_data_path: interim_data/
processed_data_path: processed_data/
headers_path: headers/
version: v1

ids_file_name: random.ids.txt
ids_has_header: False
ids_colname: 0

categorical_inputs:
  - name: random.genotypes
    weight: 1
  - name: random.drugs
    weight: 1
  - name: random.categorical
    weight: 1

continuous_inputs:
  - name: random.continuous
    weight: 2
  - name: random.transcriptomics
    weight: 1
  - name: random.diet_wearables
    weight: 1
  - name: random.proteomics
    weight: 1
  - name: random.metabolomics
    weight: 1
  - name: random.metabolomics_untarget
    weight: 1
  - name: random.metagenomics
    weight: 1

data_of_interest: random.drugs

categorical_names: ${names:${data.categorical_inputs}}
continuous_names: ${names:${data.continuous_inputs}}
categorical_weights: ${weights:${data.categorical_inputs}}
continuous_weights: ${weights:${data.continuous_inputs}}

data_features_to_visualize_notebook4:
  - drugs_1
  - continuous_2
  - continuous_3

write_omics_results_notebook5:
  - random.metabolomics
  - random.metabolomics_untarget
