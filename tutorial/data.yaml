na_value: NA

# raw - for raw data (e.g., the TSVs)
# interim - for intermediate data (e.g., the NPYs)
# processed - for processed data (e.g., trained models, latents)
raw_data_path: data/
interim_data_path: interim_data/
processed_data_path: processed_data/
headers_path: headers/
version: v1

ids_file_name: random.small.ids.txt
ids_has_header: False
ids_colname: 0

categorical_inputs:
  - name: random.small.drugs
    weight: 1

continuous_inputs:
  - name: random.small.proteomics
    weight: 1
  - name: random.small.metagenomics
    weight: 1

data_of_interest: random.small.drugs

categorical_names: ${names:${data_cfg.categorical_inputs}}
continuous_names: ${names:${data_cfg.continuous_inputs}}
categorical_weights: ${weights:${data_cfg.categorical_inputs}}
continuous_weights: ${weights:${data_cfg.continuous_inputs}}

data_features_to_visualize_notebook4:
  - drugs_1
  - proteomics_1
  - proteomics_124
  - proteomics_193
  - metagenomics_695

write_omics_results_notebook5:
  - random.small.proteomics
  - random.small.metagenomics